==============
Model Registry
==============

The model registry is a hub for storing, versioning, and sharing :doc:`model` for collaboration. The relationship between :code:`Model registry` and :code:`Models` is analogous to :code:`Docker registry` and :code:`Mocker images`.
